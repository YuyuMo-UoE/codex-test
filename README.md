# sympy-integrator

A minimal Python package that performs symbolic integration using SymPy.

## Install

```bash
pip install -e .
```

## Example

```bash
sympy-integrate "sin(x)"
```

Expected output:

```text
-cos(x)
```
